"scripts": {
  "start": "node server.js",
  "build": "mkdir -p dist/angular && cp -r ./* dist/angular/ && tar -czf /mnt/code/code.tar.gz -C /mnt/code/src .",
  "test": "echo \"Error: no test specified\" && exit 1"
}
